import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['assets-in.bmscdn.com', 'in.bmscdn.com', 'assets-in-gm.bmscdn.com', 'in-gm.bmscdn.com'],
    },
};

export default nextConfig;
